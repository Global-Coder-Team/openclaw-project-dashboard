{
  "id": "openclaw-project-dashboard",
  "name": "OpenClaw Project Dashboard",
  "description": "Local-first project dashboard with a web UI and agent-friendly commands.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": { "type": "number", "default": 5178 },
      "dbPath": { "type": "string", "default": "project-dashboard.sqlite" }
    }
  }
}
