{
  "name": "openclaw-project-dashboard",
  "version": "0.2.0",
  "description": "Local-first project dashboard OpenClaw plugin (web UI + agent commands)",
  "license": "MIT",
  "type": "module",
  "main": "index.ts",
  "openclaw": {
    "extensions": [
      "index.ts"
    ]
  },
  "scripts": {
    "test": "echo no-tests",
    "build": "echo no-build"
  },
  "dependencies": {
    "openclaw": "^2026.2.25",
    "sql.js": "^1.13.0"
  }
}
